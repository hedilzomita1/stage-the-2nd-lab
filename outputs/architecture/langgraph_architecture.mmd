---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	initializer(initializer)
	bridge(bridge)
	auditor(auditor)
	psycho(psycho)
	rhetoric(rhetoric)
	logistics(logistics)
	aggregator(aggregator)
	final_scoring(final_scoring<hr/><small><em>__interrupt = before</em></small>)
	__end__([<p>__end__</p>]):::last
	__start__ --> initializer;
	aggregator --> final_scoring;
	auditor -.-> bridge;
	auditor -.-> logistics;
	auditor -.-> psycho;
	auditor -.-> rhetoric;
	bridge --> auditor;
	initializer --> bridge;
	logistics --> aggregator;
	psycho --> aggregator;
	rhetoric --> aggregator;
	final_scoring --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
